#!/bin/bash

# Invoke extraction script
# For each hive file extracted, run hivexml
# For each regxml file generated, run xmllint to pretty-print
# When all regxml is pretty-printed, generate a database
