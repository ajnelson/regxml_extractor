- Installation feels a little strange right now with the shell script.  Either:
1.) Make this a python distutils project
2.) See how some other shell-only package handles autotool pathing (this should involve AC_CONFIG_FILES, per <http://stackoverflow.com/a/2211433/1207160>)

- Python 3

- Add an input parameter to pass disk image sequence instructions to sqlite generator

- Write a hive subset exporter; e.g. export(r"\*") would export as DFXML the whole hive's contents
